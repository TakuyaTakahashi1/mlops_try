# mlops_try sample .env
# 本番の秘密情報は絶対に入れないこと。

# アプリ環境
APP_ENV=local

# サンプル DB URL（実際の環境に合わせて書き換える）
DB_URL=postgresql://user:pass@localhost:5432/sampledb

# 外部APIなどに使うサンプルキー
API_KEY=change-me

# /version で返すバージョン例
APP_VERSION=0.1.0
